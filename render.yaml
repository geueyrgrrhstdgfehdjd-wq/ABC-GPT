services:
  - type: web
    name: bluezygpt-web
    runtime: docker
    plan: free

  - type: worker
    name: bluezygpt-bot
    runtime: node
    plan: free
    rootDir: bot
    envVars:
      - key: DISCORD_TOKEN
        value: YOUR_BOT_TOKEN
